services:
  meshcore-llm-bot:
    build: .
    container_name: meshcore-llm-bot
    restart: unless-stopped
    env_file:
      - ./env
    healthcheck:
      test: ["CMD", "ping", "-c", "1", "192.168.86.108"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
